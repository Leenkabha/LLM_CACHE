# Normalize line endings to LF in the repo and the working tree (shell scripts and
# Dockerfiles break with CRLF, and Windows/WSL checkouts otherwise churn).
* text=auto eol=lf

*.sh   text eol=lf
*.pdf  binary
*.png  binary
*.jpg  binary
*.ico  binary
